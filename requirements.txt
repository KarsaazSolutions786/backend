fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
PyJWT  # JWT token handling for Firebase auth
passlib[bcrypt]
python-decouple
APScheduler
# vosk  # Not compatible with Python 3.13. Use Python 3.10/3.11 for speech-to-text features.
# TTS>=0.21.0  # Coqui TTS - Not compatible with Python 3.13. Fallback to pyttsx3/gTTS
# transformers  # Temporarily disabled due to torch dependency
# torch  # Not compatible with Python 3.13
numpy==1.25.2
pandas==2.1.4
scikit-learn==1.3.2
python-dotenv==1.0.0
requests==2.31.0
aiofiles==23.2.1
# Coqui STT for speech-to-text
# stt  # Temporarily disabled due to compatibility
# Audio processing dependencies - Python 3.13 compatible
librosa==0.10.1
soundfile==0.12.1
# Alternative speech recognition - Python 3.13 compatible
SpeechRecognition==3.10.0
# Alternative TTS solutions - Python 3.13 compatible
pyttsx3
gTTS
# For microphone support (optional)
# pyaudio  # Temporarily disabled
pydantic-settings==2.1.0
# AI services
openai
httpx==0.25.2

# Audio processing libraries
pydub==0.25.1
scipy==1.11.4

# ML/AI libraries
accelerate==0.24.1
datasets==2.14.7
evaluate==0.4.1

# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1

# PyTorch and Transformers (with CPU-only support for compatibility)
torch==2.1.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
transformers==4.35.2
tokenizers==0.14.1

# Additional dependencies for enhanced functionality
asyncpg==0.29.0
firebase-admin==6.2.0 