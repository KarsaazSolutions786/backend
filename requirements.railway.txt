# Minimal requirements for Railway deployment
# Core FastAPI and database dependencies only
# NOTE: Mini_LM.bin model is tracked with Git LFS to avoid build-time downloads

# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Pydantic for data validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0  # Required for Pydantic email validation

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication and security
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4

# HTTP and file handling
httpx==0.25.2
aiofiles==23.2.1
python-multipart==0.0.6
requests==2.31.0

# Configuration
python-dotenv==1.0.0
python-decouple==3.8

# Audio processing dependencies (FIXED - was missing these)
numpy==1.24.4
scipy==1.11.4
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
SpeechRecognition==3.10.0

# Scientific computing
scikit-learn==1.3.2

# PyTorch and Transformers (CPU-only version for Railway)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
transformers==4.35.2
tokenizers==0.14.1

# Utilities
APScheduler==3.10.4

# AI services
openai==1.3.7

# Additional dependencies
python-dateutil==2.8.2
firebase-admin==6.2.0
asyncpg==0.29.0 